<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="google" content="notranslate" />
  <title>MapMap login</title>
  <meta name="description" content="MapMap lets you do fun stuff with maps." />
  <meta name="viewport"
    content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, shrink-to-fit=no" />

  <link rel="stylesheet" href="/styles/main.css" type="text/css" />
  <link rel="stylesheet" href="/styles/layout.css" />
</head>

<body>
  <%- include('partials/header') %>

    <div class="login">
      <h1>Login to MapMap</h1>
      <form method="POST" action="/users">
        <label for="user_id">Select your user:</label>
        <select name="user_id" id="user_id" required>
          <% users.forEach(user=> { %>
            <option value="<%= user.id %>">
              <%= user.name %> (<%= user.email %>)
            </option>
            <% }) %>
        </select>
        <input type="submit" value="Login" />
      </form>
    </div>

    <div class="favourites" style="display: flex; flex-direction: column; align-items: center; justify-content: center; margin-top: 2rem; text-align: center;">
  <h2>View your favourite map here</h2>
  <a href="/users/favourites" style="margin-top: 1rem; padding: 0.6rem 1.2rem; background-color: #17b585; color: white; text-decoration: none; border-radius: 5px; font-weight: bold;">Go to Favourites</a>
</div>

</body>

</html>
